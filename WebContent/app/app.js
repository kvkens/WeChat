/**
 * requireJS Config
 */
requirejs.config({
	//baseUrl : "app",
	paths : {
		jquery : "../lib/jquery/jquery.min",
		bootstrap : "../lib/bootstrap/bootstrap.min"
	}
});

require(["jquery","bootstrap","debug"],function($){
	
});